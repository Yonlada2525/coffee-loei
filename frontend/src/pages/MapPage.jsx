
import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet'; import 'leaflet/dist/leaflet.css'; import L from 'leaflet'; import { useEffect,useState } from 'react'; import { api,fileUrl } from '../utils/api';
delete L.Icon.Default.prototype._getIconUrl; L.Icon.Default.mergeOptions({iconRetinaUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png',iconUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png',shadowUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png'});
export default function MapPage(){ const [rows,setRows]=useState([]); useEffect(()=>{api('/farms').then(setRows)},[]); return <main className="shell py-8"><section className="hero p-7"><span className="chip">Coffee Map</span><h1 className="mt-4 text-4xl font-black text-coffee">แผนที่สวนกาแฟ</h1><p className="mt-3 text-[#786a62]">คลิกหมุดเพื่อดูรูปภาพ เจ้าของสวน พื้นที่ พันธุ์กาแฟ และรายละเอียด</p></section><section className="card p-3 mt-8"><MapContainer center={[17.486,101.72]} zoom={9} scrollWheelZoom={true}><TileLayer attribution="&copy; OpenStreetMap" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{rows.filter(f=>f.latitude&&f.longitude).map((f,i)=><Marker key={f.farm_id} position={[f.latitude,f.longitude]}><Popup maxWidth={330}><div className="w-[280px] font-prompt"><img className="w-full h-40 object-cover rounded-3xl mb-3" src={fileUrl(f.file_path||`frontend/assets/images/farm-${i%5+1}.svg`)}/><h3 className="text-xl font-black text-coffee">{f.farm_name}</h3><p className="text-sm text-[#786a62]">{f.village} ต.{f.sub_district} อ.{f.district} {f.postal_code}</p><div className="flex flex-wrap gap-2 my-3"><span className="chip">{f.coffee_name||'ไม่ระบุ'}</span><span className="chip">{f.area_size||0} ไร่</span><span className="chip">{Number(f.water_system)?'มีระบบน้ำ':'ไม่มีระบบน้ำ'}</span></div><p><b>เจ้าของ:</b> {f.fullname||'-'}</p><p><b>ดิน:</b> {f.soil_type_name||'-'}</p><p><b>ความสูง:</b> {f.altitude||'-'} ม.</p><p className="mt-2 bg-mint/50 rounded-2xl p-3 text-sm">{f.description||'ไม่มีรายละเอียด'}</p></div></Popup></Marker>)}</MapContainer></section></main> }
