import{useEffect,useState}from'react';import{API,asset}from'../services/api';import{LOEI}from'../data/locations';
export default function PublicFarms(){const[rows,setRows]=useState([]);const[d,setD]=useState('');useEffect(()=>{API.get('/farms'+(d?`?district=${encodeURIComponent(d)}`:'')).then(setRows)},[d]);return <div className="shell"><section className="hero p-6"><h1 className="text-4xl font-black text-coffee">สวนกาแฟในจังหวัดเลย</h1><p className="mt-2 text-[#786a62]">ค้นหาและดูรายละเอียดสวนกาแฟพร้อมรูปภาพ</p><select className="select mt-5 max-w-xs" value={d} onChange={e=>setD(e.target.value)}><option value="">ทุกอำเภอ</option>{Object.keys(LOEI).map(x=><option key={x}>{x}</option>)}</select></section><section className="grid md:grid-cols-3 gap-5 mt-8">{rows.map((f,i)=><article className="card p-4" key={f.farm_id}><img className="image-card" src={asset(f.file_path||`/assets/images/farm-${(i%5)+1}.svg`)}/><h3 className="mt-4 text-xl font-black text-coffee">{f.farm_name}</h3><p className="text-[#786a62]">{f.village} ต.{f.sub_district} อ.{f.district}</p><div className="mt-3 flex gap-2 flex-wrap"><span className="chip">{f.coffee_name||'-'}</span><span className="chip">{f.area_size||0} ไร่</span><span className="chip">{f.altitude||'-'} ม.</span></div><p className="mt-3 text-sm leading-7 text-[#786a62]">{f.description}</p></article>)}</section></div>}
