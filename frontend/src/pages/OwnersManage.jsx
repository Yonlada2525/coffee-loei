import{useEffect,useState}from'react';import{API}from'../services/api';
export default function OwnersManage(){const[rows,setRows]=useState([]);const[form,setForm]=useState({username:'',password:'admin123',fullname:'',phone:'',email:'',address:'',status:'approved'});const load=()=>API.get('/owners').then(setRows);useEffect(load,[]);async function submit(e){e.preventDefault();await API.post('/owners',form);setForm({...form,username:'',fullname:'',phone:'',email:'',address:''});load()}return <div className="shell"><section className="hero p-6"><h1 className="text-4xl font-black text-coffee">จัดการเจ้าของสวน</h1></section><form onSubmit={submit} className="card p-5 mt-8 grid md:grid-cols-3 gap-4">{Object.keys(form).map(k=><label key={k}><span className="label">{k}</span><input className="input" value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="btn btn-primary md:col-span-3">เพิ่มเจ้าของสวน</button></form><div className="table-wrap mt-8"><table><thead><tr><th>ชื่อ</th><th>ติดต่อ</th><th>สถานะ</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.owner_id}><td><b>{o.fullname}</b><br/>{o.username}</td><td>{o.phone}<br/>{o.email}</td><td><span className="chip">{o.status}</span></td><td><button className="btn btn-ghost" onClick={async()=>{if(confirm('ลบ?')){await API.delete('/owners/'+o.owner_id);load()}}}>ลบ</button></td></tr>)}</tbody></table></div></div>}
